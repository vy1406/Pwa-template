Once you add the svg inside svgs folder
run the command:
npm run svg.

it will automatically create the file, based on file name
and will import it in index.js, so you can import it as react component.